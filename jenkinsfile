node {
    def rootDir = pwd()
    def example = load "${rootDir}@script/pipeline.Groovy"
    example.exampleMethod()
	def notify = load "${rootDir}@script/notifyslack.Groovy"
    notify.call()
}