pipeline {
    agent any
    stages {
        stage("build") {
            steps {
                sh 'mvn -Dmaven.test.skip=true site'
            }
        }
    }
    post {
        always {
            archive "target/**/*"
            junit 'target/surefire-reports/*.xml'
        }
    }
}