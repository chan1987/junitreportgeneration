pipeline {
    agent any
    stages {
        stage("build") {
            steps {
                sh '-Dmaven.test.skip=true surefire-report:report'
            }
        }
    }
    post {
        always {
            archive "target/**/*"
            junit 'target/surefire-reports/*.xml'
        }
    }
}